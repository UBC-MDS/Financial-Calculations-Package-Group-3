import plotly.express as px

def plot_break_even_point(fixed_costs, sales_price_per_unit, variable_cost_per_unit, max_units):
    """
    Plot the break-even point.
    Parameters:
    fixed_costs (float): Total fixed costs.
    sales_price_per_unit (float): The selling price of each unit.
    variable_cost_per_unit (float): The variable cost of each unit.
    max_units (int): Maximum number of units to plot.
    Returns:
    None: The function will plot the break-even graph.
    """